import 'package:flutter/material.dart';

class FriendFilter extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('This is Friends Filter'));
  }
}